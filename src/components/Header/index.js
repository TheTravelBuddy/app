import React from "react";
import { Header as Appbar } from "react-native-paper";

const Header = () => {
  return (
    <Appbar.Header>
      <Appbar.Content title="Travel buddy" />
    </Appbar.Header>
  );
};

export default Header;
